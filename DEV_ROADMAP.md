# 多阶段开发规划（基于 12 维度分析）

> 更新：2026-09-06。依据 `DEV_ANALYSIS.md`（12 域整体 ≈93.2%，满格 6 域，主攻区 = 域 3 网格规律 / 域 4 求解器 / 域 1·2·10 数据尾账）。
> 阶段编号沿用 Phase 惯例：本规划 = **Phase K1→K6**，排期按单人手速估计；每阶段含量化验收，完成即回写 REVERSE_STATUS.md。

## K1 — 数据尾账收官（域 1/2/10 → 100%）

**目标**：把数据层 D5 级残余与变量接入补完，域 2 90→100、域 1 95→100、域 10 95→100。

| # | 工作项 | 目标 | 产出 |
| --- | --- | --- | --- |
| K1-1 | **E1c grid_output 尾表**：描述符之后嵌套邻接/区域表（面 id 对 + 双精度区）解码 | 全节解码 D5 | fluent_grid 扩展 + 与 cas 面邻接对拍 |
| K1-2 | **problem 数组深层语义**：gradient/transient 表、列表/嵌套值完整建模 | 26 工程零未知键 + 语义访问器全覆盖 | problem_parser/访问器扩展 + 26 工程回扫 |
| K1-3 | **老欠账**：多 shape 对象（保留最后一个 → 全量）、shape_polygon 顶点提取、多边形状几何（hexa/quad/plate 之外的 pts 通用化） | 26 工程几何量 1247 对象全提取 | model_parser/export 扩展；GUI 几何构建率 1195/1247 → 1247/1247 |
| K1-4 | **fdat 变量级 UI**：后处理 Select variable 下拉（SV_P/U/V/W/DENSITY/HEAT_FLUX/… + _M1 瞬态前步），云图/曲线按变量 | 8+ 变量可视选择 | `_maybe_real_post_actor` 参数化 + PlotWindow 变量通道 |
| K1-5 | **云图 → PlotWindow 衔接**：真实温度云统计出图（均值/极值/直方图直出）完整接线 | 报告/图窗一键 | ice_report 扩展 + GUI |

**验收**：K1-1 尾表区与 cas 逐项对拍一致；K1-2 26 工程零未知键；K1-3 几何提取 1247/1247；K1-4 任意变量可渲染；全套 396+ 测试全绿。

## K2 — 求解器深水（域 4 80 → 95）

**目标**：自研 heat_solver 与 oracle 区域级比对 <5%；瞬态内核；ROM/优化真实化。

| # | 工作项 | 目标 | 产出 |
| --- | --- | --- | --- |
| K2-1 | **精网格真实 BC**：refine/HDM 网格 + 逐对象材料导热/体源/convection/辐射的 real_problem 装订 | 10-1/7-1/12-1 温度场区域均值/极值偏差 <5% | heat_solver + problem→BC 装订器 |
| K2-2 | **区域级比对**：分区（solid/fluid/壳体/柱域）mean/|ΔT| 对拍（现有分布级 → 区域级） | heat_solver_compare 升级 + golden 表 |
| K2-3 | **瞬态求解内核**：时间推进（显式/隐式 SOR + 材料比热/密度），对接 fdat `_M1` 多步场 | 10-1 瞬态 3 步残差趋势对拍 | heat_solver.transient + fdat 多步验证 |
| K2-4 | **ROM/优化真实化**：Krylov ROM 线性化场构建（现仅 UI）、trials 批量执行实化 | Define trials → 真实批量求解 + 汇总 | ice_solve ROM 引擎 |

**验收**：K2-1/2 与 oracle 区域偏差 <5%（口径：均值 ±5% 且极值 ±10%）；K2-3 瞬态 3 步无发散且趋势一致；K2-4 trials 批量 3+ 组自动化。

## K3 — 网格规律收编（域 3 law 66 → 85+）

**目标**：执行 B 路（DEV_PLAN.md P1–P4），用规律替换金表。

| # | 工作项 | 目标 | 产出 |
| --- | --- | --- | --- |
| K3-1 | **P1 7-2 逐柱归属**：33 柱/30 锥逐柱尾面/簇层归属，m 随 (r,h) 统计 | TAIL 几何来源线索 | cone_map + m 分布表 |
| K3-2 | **P2 9-2 反例解剖**：5 圆柱 tail=0 判据（半径/方向/位置） | 尾族出现判据（4 工程互证） | 判据函数 + 回归 |
| K3-3 | **P3 11-1 提取修复 + 补扫** | 语料 +1 | j2b_corpus 更新 |
| K3-4 | **TAIL 封闭式**：1e-12 命中的几何参解析式（P1/P2 数据驱动）或**证明为程序常量**（需 P4 环境） | 目标实现则定律收口 | TAIL_FORMLINE + 测试 |
| K3-5 | **簇层 xy 生成器**：以 z 态切片律重构 125 簇层 (x,y) | 簇层 xy 全公式化 | ice_hdm_layers.dual 生成器 |
| K3-6 | **mesher 沙箱**（许可可获时）：icb→mesher 任务对拍节点/残差 | 真实 mesher 对接 (rc=0) | probe 扩展 + golden |

**验收**：K3-4/5 达成则 34% 金表 50%+ 被规律替换（law 66→85+），as-built 残差表收缩；P1–P3 数据耗尽仍不闭合 TAIL 时按 DEV_PLAN 收口（判据 + 调参 + 待环境）。

## K4 — 后处理渲染深水（域 10 95 → 100）

| # | 工作项 | 目标 |
| --- | --- | --- |
| K4-1 | 矢量场云图 UI（速度箭头密度/缩放/着色变量） | 124k 箭头交互参数化 |
| K4-2 | 瞬态动画（fdat 多步 → vtk 时间序列 → Gif/视频） | 动画链路 |
| K4-3 | 报告真实化补全（云图段/动画帧/变量筛选入 HTML） | write_real_report 全变量 |
| K4-4 | 后处理单位换算（SOLUTION_UNITS_KEYS → 实际渲染换算） | 单位一致 |

**验收**：K4-1/2 端到端可交互；K4-3 报告含变量表；K4-4 云图与报告单位同源。

## K5 — GUI 残余与像素级回归（域 5 97 → 100）

| # | 工作项 | 目标 |
| --- | --- | --- |
| K5-1 | `_on_lib_activated` 库实例化接入（D2b 向导路由） | NYI 真残留清零 |
| K5-2 | spreadsheet 单元格类型化残余（数值/枚举单元格编辑控件） | 批量编辑 100% 类型化 |
| K5-3 | 树图标按键对齐终审（golden 树图标键 70 枚全对齐） | golden 对表 |
| K5-4 | 像素级截图回归（真实显示环境条件项）：重启 screenshot_regression grab 通道 | 像素 diff 基线 |

**验收**：K5-1/2/3 后 `grep _nyi` 守卫外清零；K5-4 在具备显示的主机跑通（无显示则保持结构快照口径，记录环境限制）。

## K6 — 验证与发布收口

| # | 工作项 | 目标 |
| --- | --- | --- |
| K6-1 | 全工程字节级写回重验（18/18 model + ICB 族） | 0 delta |
| K6-2 | 16 工程网格计数/复刻全量 re-run（P17 exact + as-built 主机验证） | 全部 0.0000000% / 150 层复刻 |
| K6-3 | 26 工程扫描 + 19 工程 3D 回归重跑 | 零未分类/零崩溃 |
| K6-4 | CI 分片扩展（J 阶段测试并入分片矩阵 + --all 全绿） | 8 片 ALL PASS |
| K6-5 | 资产/文档收口（PARITY_GAP_ANALYSIS 刷新 12 维表 + README 基线数字更新） | 文档与代码一致 |

**验收**：K6-1..4 全绿；K6-5 后 DEV_ANALYSIS 的 12 维表与实测一致。

## 排期与依赖

| 阶段 | 依赖 | 预估 |
| --- | --- | --- |
| K1 | 无（纯数据层） | 1–1.5 周 |
| K2 | K1-4 部分（fdat 多步） | 1.5–2 周 |
| K3 | DEV_PLAN P1–P3；P4 需 Icepak 环境/许可（条件项） | 1–1.5 周（数据驱动） |
| K4 | K1-4 | 0.5–1 周 |
| K5 | 无 | 0.5 周 |
| K6 | K1–K5 | 0.5 周 |
| 合计 | | 约 5–6.5 周 |

## 优先级建议

1. **K3 优先**（研究性最重、数据驱动、历史瓶颈；K3-1/2/3 无环境依赖可立即启动）。
2. **K1 其次**（纯工程量、直接推 3 个域到 100%、且为 K2/K4 的前置）。
3. K2（与 K1-4 交错）、K4、K5、K6 按序。
