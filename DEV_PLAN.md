# 开发计划 — B 路线数据瓶颈攻坚

> 更新：2026-09-06
> 关联：DEV_SUMMARY.md（A/B 现状与瓶颈）、docs/PLAN_100PCT.md（Phase J）、REVERSE_STATUS.md（J1a→J3-A）。
> 主题：B 路线（过渡带/圆柱局域生成律）当前卡在**数据约束**——本计划列出数据瓶颈、突破路径、优先级与验收。

## 1. 总目标

把 HDM 过渡带/圆柱局域结构从「金表 + 未闭包」提升为公式化生成，目标如下（按可验证性排序）：

1. **TAIL 封闭式**：TAIL = 0.000985281374239 的无参/几何参解析式（或证明其为程序常量）。
2. **簇层 xy 生成器**：10-1 的 125 簇层 + 2 special 层的 (x,y) 集生成公式。
3. **尾族出现判据**：什么几何条件下 z 面产生尾面（解释 9-2 五柱 tail=0 反例）。

## 2. 数据瓶颈（与证据现状）

| # | 瓶颈 | 现状证据 | 缺失 |
| --- | --- | --- | --- |
| B1 | TAIL 恒值样本不足 | 3 工程 / 8 尾值；闭包检验全部负结果（最近 err 4.5e-5） | 更多独立尾值（不同几何参数）以隔离相关系数；同一工程多解系（DO/norad/Ray/S2S）可作免费变体 |
| B2 | 簇层 xy 单工程 | 簇层结构仅 10-1 有完整取（125 层；xy=模板 z 态切片已证部分） | 第二锥类工程的逐 (z, xy) 簇层对标（7-2Heat-pipe 33 柱可为第一对照系） |
| B3 | 反例未解释 | 9-2Optimization 5 圆柱 tail=0（j2b_corpus.json） | 9-2 的柱几何/位置 vs 8-1/7-2 的差异分析（半径？方向？位置？z 域边界？） |
| B4 | 7-1 系提取 | 已修复（base 36,482 + sub_assembly 93,496 → 129,952；300 层；无柱 tail=0） | 无（该瓶颈已解除；留档） |
| B5 | 11-1 z 解析 | compack 子目录结构差异导致 cyls=-1 | 修 11-1compact-package 的 grid_output 定位（子目录内路径规则） |

## 3. 突破路径（按优先级）

### P1（高）：7-2Heat-pipe 逐柱归属 + 同工程多解系对照（免费变体）
- 做法：7-2 的 33 柱/30 锥按 (x,y) 平面位置归属——每柱附近的尾面/簇层因柱局部化；DO/norad/Ray/S2S 同解系（网格同、物理不同）已验证 mesh 相同（文件名与大小一致），可确认网格级变体仅 7-2 现成。
- 产出：m 分布随 (r, h, 方向) 的统计表单；若圆锥（r1≠r2）与纯圆柱的 m 集不同，直接指向 TAIL 的几何来源。
- 验收：m 对 (r, h) 的可分函数（如 m ≈ round(r/H2)、round(h/H2·…)）。

### P2（中）：9-2Optimization 反例解剖
- 做法：提取 9-2 的 5 圆柱几何（半径/方向/长度与 8-1 同表比）；确认 9-2 的 z 层集里是否存在「应该出现但消失」的尾族标志（无 tail 有两种含义：未生成 / 生成但被合并）。
- 产出：尾族出现判据候选（如仅竖直圆柱产生？仅 r≤0.015？仅圆柱穿过域心 z 域？）。
- 验收：判据在 10-1/7-2/8-1/9-2 四工程上两向一致。

### P3（中）：11-1compact-package 提取修复 + 补扫
- 做法：定位 compack 子目录的 grid_output/nodemap（fine_batch 已记录「嵌套 job 子目录」处理方式），把 11-1 拉入语料。
- 产出：语料 +1；若 11-1 有柱状体（BGA 球阵/柱引脚），尾族判定再 +N 样本。
- 验收：j2b_corpus.json 里 11-1 行 z 解析成功。

### P4（低/长期）：mesher 级或新工程取证
- 背景：TAIL 若是程序常量（如平局/舍入规则产物），纯输出数据分析无法完全隔离，需：① 新造小工程（Icepak GUI 生成，如 8-1 变体半径 0.015→0.018）——需要 Icepak 使用环境；② 或 mesher 可执行探针（ecad_oracle_probe 记录 rc=1 许可受限，未具备）。
- 前置：无；作为 P1/P2 未闭合时的后备。

## 4. 排期与依赖

| 步骤 | 依赖 | 预估 |
| --- | --- | --- |
| P1 | 7-2 网格提取（已通，140,028 节点） | 0.5–1 天 |
| P2 | 9-2 网格提取（已通，36,920） | 0.5 天 |
| P3 | 11-1 子目录定位 | 0.5 天 |
| P4 | Icepak 环境/许可 | 无期（条件项） |

## 5. 验收口径

- B 路完成 = TAIL 封闭式命中（1e-12 级）**或** 尾族出现判据在 4 工程互证 + m 函数化。
- 所有中间产物落 tools/probe_work/*.json（既有风格）；状态回写 REVERSE_STATUS.md。
- 若 P1–P3 耗尽仍无法隔离 TAIL：B 路以「判据 + 调参闭包 + 待环境」收口，转移到 P4 前置（Icepak 环境），并把 as-built 34% 金表状态记录为既定终态。
