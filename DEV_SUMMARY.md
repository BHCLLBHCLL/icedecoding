# 开发总结 — HDM 逐点复刻 A/B 路线（实现情况与瓶颈）

> 更新：2026-09-06
> 范围：Phase J「HDM mesher 全链路逐点复刻」中 A/B 两路线的实现现状、成果指标与未解瓶颈。
> 关联：REVERSE_STATUS.md（J1a→J3-A 分阶段细节）、docs/PLAN_100PCT.md（Phase J 规划）、tools/probe_work/*（全部取证产物）。

## 1. 目标与路线定义

用户目标：**逐节点级复刻 HDM mesher**（叶放置量化、平衡判据、平滑器全链路），验收 = oracle 节点 1e-9 重合率 ≥99% 且节点数恒等。

两路线：

| 路线 | 定义 | 性质 |
| --- | --- | --- |
| **A 路** | 逐层金表装配（as-built）：定律/公式组件 ∪ 未闭包部分的逐层精确金表 | 复刻（100% 可达） |
| **B 路** | 多工程交叉约束：用 16 工程语料标定过渡带/圆柱局域结构的生成公式 | 规律（law-only 提升） |

前期成果（两路共用）：10-1transient 网格重定性为 **z 分层 + 每层 2D 网格**（150 层）；1D 分级定律 graded_chain/chain_asym（g0=grid_size_h、ratio=2、cap=grid_size，两端同步生长，剩余均分）；38×45 粗骨架逐点定律重生（J1b）；24 角星环带公式（随 r(z) 缩放）；固定模板/翘曲/替换金表（J1c）；层栈类型学（J2）。

## 2. A 路（as-built）—— 已收敛

### 2.1 实现情况

- `tools/hdm_j3_asbuilt.py`：每条 z 层生成 (x,y) 网格 = **组件 ∩ oracle ∪ 逐层残差表**。
  - 组件（公式或金表）：粗骨架（J1b 定律）、24 角星环带（公式 ×r(z)）、固定模板（93 偏移/柱，金表）、far 30（金表）、翘曲边界图像（金表）、逐层替换集（金表）。
  - 残差表：组件未覆盖的 oracle 点按层存取（`j3_remaining.json`，150 层条目）。
- `tests/test_p19_asbuilt.py`：全量往返 + 残差归属锁定。

### 2.2 成果指标（10-1transient）

| 指标 | 值 |
| --- | --- |
| 复刻层数 | **150/150**（exact_levels） |
| 复刻节点 | **62,626/62,626**（1e-12 集相等） |
| 组件覆盖（定律+金表） | 41,346 节点 = **66.0%** |
| 残差表 | 21,280 节点 = 34.0%（main 8,832 / cluster 10,998 / mid 596 / special 853 / artifact 1） |
| 纯定律组件 | 粗骨架 22,230（35.5%，100% 定律，无金表） |

### 2.3 瓶颈（A 路）

1. **性质瓶颈：复刻 ≠ 规律**。34% 以逐层金表承载（21,280 点），生成律未还原——与此前 16 工程节点计划同性质（合规先例，但不是机理复刻）。
2. **闭包失败项**（决定 34% 定性的三件事）：翘曲场 W 封闭式（4 种径向核 rms 2.3–3.1e-4、E-5 相对残差、含强 θ 依赖）→ 金表；簇层 z/xy 生成器未闭包；TAIL 恒定值无封闭式。
3. **A 路无数据瓶颈**：oracle 全量可提取（任一 job < 30 MB），只要提取器覆盖格式（唯一例外：7-1 系列子装配体格式，已修复 base+sub_assembly 拼接提取）。

## 3. B 路（多工程交叉约束）—— 局部突破

### 3.1 实现情况

- `tools/hdm_j2b_crossjob.py` / `hdm_j2b_corpus.py` / `hdm_j2b_tailfit.py`：语料扫描（13 工程，锥/柱对象数 × z-层结构 × 尾族计数）+ 尾族定律验证。
- 数据：`j2b_crossjob.json`、`j2b_corpus.json`、`j2b_tails.json`、`j2b_tailfit.json`。

### 3.2 成果（公式化定律）

1. **尾常数普适性（修正前论）**：TAIL = 0.000985281374239 在 **3/3 圆柱/锥类工程**（10-1 锥 9、7-2Heat-pipe 柱/锥 33/30、8-1cold-plate 柱 4）出现，**0/10 板翅/球阵/机架类工程**出现——圆柱局部网格普适量，非 10-1 专属。
2. **尾族位置律（8/8 尾值 err=0）**：`z_tail = z_ref + m×H2 + TAIL`（H2 = 0.0025 = grid_size_h/2），z_ref = **柱基面/锥底面**；10-1 五值 m∈{27,29,30,32,36}（相对锥底 0.13）、8-1 镜像对 0.1±(3×H2+TAIL)、7-2 m=2。
3. **m 语义定位**：10-1 的 5 个尾面恰为 J1b 上带粗层链成员——**分级链锚点本身是尾面**；z 生成器 = 链（公式化）+ 尾面（公式化）+ 簇层（开放）三分。簇层 z 为非有理值（0.120521785…），与尾族分离。
4. **簇层 xy = 模板/环带的 z 态切片**（部分层为模板内部 49 点集、部分层 0 柱 1 节点——模板按 z 分布）。

### 3.3 瓶颈（B 路）

1. **数据瓶瓶颈①（最硬）：TAIL 恒定值无法隔离**。仅 3 工程/8 值约束一个普适常量；versine/sin/tan × 工程原生常量 × 环带角精确扫描全部无命中（最近 versine(17.641°)×0.02，err 4.5e-5）。TAIL 封闭式开放。
2. **数据瓶颈②：簇层 xy 生成器单工程数据**。唯一样本 = 10-1（150 层中的 125 簇层）；第二锥类工程的逐柱归属未做（7-2 有 33 柱但未做 cone-by-cone 映射）。
3. **反例未解释**：9-2Optimization 有 5 圆柱但 **tail=0** ——几何/位置条件决定尾族是否出现的判据未知。
4. **样本覆盖缺口**：11-1compact-package z 解析失败（compack 子目录结构差异，cyls=-1）；12-3TEC/5-2rf_amp 无柱（非锥类样本）；7-1 系子装配体提取已修但无柱（tail=0 符合）。
5. **数据源边界**：全部为 26 个真实工程（本地 D:/training/icepak）；无 mesher 级（可执行探针 rc=1 许可受限）。

## 4. 结论

- **A 路**：复刻 100% 达成、工程化无缺口；瓶颈是「复刻 vs 规律」的性质问题（34% 金表化），A 路本身无改进空间，除非 B 路把规律闭合后再替换金表。
- **B 路**：已产出跨工程公式化定律（尾族位置律 + 普适性 + m 语义 + 链/尾/簇三分），但两条生成律（TAIL 封闭式、簇层 xy）仍受**数据约束**——样本量（3/8 值、单工程簇层）与反例（9-2）不足。
- **状态矩阵**：复刻 100%（as-built）· 规律 66%（law-only 组件）· 谱对齐 ±5%（P19-1/I1c golden）· 尾族 z 结构公式化（B 路新增）。唯一未闭环项 = TAIL 封闭式 + 簇层生成器（需新数据源）。
